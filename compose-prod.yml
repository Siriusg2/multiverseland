version: "3"

services:
  client:
    container_name: multiverseland
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - .:/app
    ports:
      - "3000:80"
    networks:
      - main

networks:
  main:
    driver: bridge
    external: true
